-- Optional notes for accounts (users / customers) already defined in 001_initial_schema.sql
-- This migration is a no-op marker for account management readiness.
-- File fallback: backend/data/auth-store.json when PostgreSQL is offline.

SELECT 1;
